# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		python-nab-index
VERSION=		0.0.12
KEYWORDS=		python
VARIANTS=		v13 v14
SDESC[v13]=		Simple-API client and on-disk cache (3.13)
SDESC[v14]=		Simple-API client and on-disk cache (3.14)
HOMEPAGE=		https://github.com/notatallshaw/nab
CONTACT=		Python_Automaton[python@ravenports.com]

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPIWHL/a4/f5/2cd88ed7005f887c35cb404d7a7064ef0518197f66ae24ed575f0f021ee1
DISTFILE[1]=		nab_index-0.0.12-py3-none-any.whl:main
DIST_SUBDIR=		python-src
DF_INDEX=		1
SPKGS[v13]=		single
SPKGS[v14]=		single

OPTIONS_AVAILABLE=	PY313 PY314
OPTIONS_STANDARD=	none
VOPTS[v13]=		PY313=ON PY314=OFF
VOPTS[v14]=		PY313=OFF PY314=ON

DISTNAME=		nab_index-0.0.12.dist-info

GENERATED=		yes

[PY313].RUN_DEPENDS_ON=			python-packaging:single:v13
					python-truststore:single:v13
					python-typing-extensions:single:v13
					python-urllib3:single:v13
[PY313].USES_ON=			python:v13,wheel

[PY314].RUN_DEPENDS_ON=			python-packaging:single:v14
					python-truststore:single:v14
					python-typing-extensions:single:v14
					python-urllib3:single:v14
[PY314].USES_ON=			python:v14,wheel

[FILE:775:descriptions/desc.single]
# nab-index

PyPI Simple-API client and on-disk cache used by
[`nab-python`] and
[`nab`].

It provides:

- A small async transport interface with two drop-in backends:
  - `urllib3` (default; pulled in by the base install).
  - `httpx` (extra: `nab-index[httpx]`).
- A Simple-API client with JSON and HTML decoders.
- A disk cache for project listings and file metadata responses.
- A multi-index router (ordered named indexes plus per-package
  overrides guarded by PEP 508 markers).
- A small VCS clone helper used by the higher-level VCS policy.

## When to use it

Use `nab-index` if you need a typed PyPI Simple-API client with an
on-disk cache.  Most users want
[`nab`] instead.

The API is currently under rapid experimentation, if you use it
pin to an exact version.


[FILE:123:distinfo]
f4d49401af187b677caff48437a45727af84232d2900faee39fe2b466ee38ee2        63960 python-src/nab_index-0.0.12-py3-none-any.whl

