# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		py27-setuptools
VERSION=		46.4.0
KEYWORDS=		python devel sysutils
VARIANTS=		standard
SDESC[standard]=	Python package development process library (PY 27)
HOMEPAGE=		https://github.com/pypa/setuptools
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPI/s/setuptools
DISTFILE[1]=		setuptools-46.4.0.zip:main
DF_INDEX=		1
SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILDRUN_DEPENDS=	python27:single:standard

USES=			cpe

DISTNAME=		setuptools-46.4.0

LICENSE=		MIT:single
LICENSE_FILE=		MIT:{{WRKSRC}}/LICENSE
LICENSE_SCHEME=		solo

CPE_PRODUCT=		setuptools
CPE_VENDOR=		python

MAKE_ENV=		LDSHARED="{{CC}} -shared"
			PYTHONDONTWRITEBYTECODE=
			PYTHONOPTIMIZE=

PLIST_SUB=		STVER=46.4.0
			PYVER=2.7
			SITEPKG=lib/python2.7/site-packages

do-build:
	(cd ${BUILD_WRKSRC} && ${SETENV} ${MAKE_ENV} \
		${PYDISTUTILS_SETUP} build)

do-configure:
	(cd ${BUILD_WRKSRC} && ${SETENV} ${MAKE_ENV} \
		${PYDISTUTILS_SETUP} config)

do-install:
	(cd ${INSTALL_WRKSRC} && ${SETENV} ${MAKE_ENV} \
		${PYDISTUTILS_SETUP} install \
		--record ${WRKDIR}/.PLIST.pymodtmp -c -O1 \
		--prefix=${PREFIX} --single-version-externally-managed \
		--root=${STAGEDIR})
	${RM} ${STAGEDIR}${PREFIX}/bin/easy_install

[FILE:219:descriptions/desc.single]
'Setuptools' is a collection of enhancements to the Python 'distutils'
for Python 2.7 that allow developers to more easily build and distribute
Python packages, especially ones that have dependencies on other packages.


[FILE:100:distinfo]
4334fc63121aafb1cc98fd5ae5dd47ea8ad4a38ad638b47af03a686deb14ef5b       865912 setuptools-46.4.0.zip


[FILE:79:manifests/plist.single]
@comment site-package directory list auto-generated
bin/easy_install-%%PYVER%%


[FILE:382:patches/patch-pkg__resources_____init____.py]
--- pkg_resources/__init__.py.orig	2020-05-17 02:30:06 UTC
+++ pkg_resources/__init__.py
@@ -83,7 +83,6 @@ __import__('pkg_resources.extern.packagi
 __import__('pkg_resources.extern.packaging.specifiers')
 __import__('pkg_resources.extern.packaging.requirements')
 __import__('pkg_resources.extern.packaging.markers')
-__import__('pkg_resources.py2_warn')
 
 
 __metaclass__ = type


[FILE:417:files/special.mk]
PYDISTUTILS_SETUP= ${LOCALBASE}/bin/python2.7 -c "import sys; \
import setuptools; \
__file__='setup.py'; \
sys.argv[0]='setup.py'; \
exec(compile(open(__file__, 'rb').read().replace(b'\\r\\n', b'\\n'), __file__, 'exec'))"

POST_PLIST_TARGET=	setuptools-autolist

setuptools-autolist:
	@(cd ${STAGEDIR}${PREFIX} && ${FIND} lib \
	\( -type f -o -type l \) 2>/dev/null | ${SORT}) \
	>> ${WRKDIR}/.manifest.single.mktmp

