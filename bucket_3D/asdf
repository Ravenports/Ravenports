# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		asdf
VERSION=		3.3.4
KEYWORDS=		devel lang
VARIANTS=		standard
SDESC[standard]=	Another System Definition Facility for Common Lisp
HOMEPAGE=		https://common-lisp.net/project/asdf/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		https://common-lisp.net/project/asdf/archives/
DISTFILE[1]=		asdf-3.3.4.tar.gz:main
DF_INDEX=		1
SPKGS[standard]=	complete
			primary
			docs

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		MIT:primary
LICENSE_FILE=		MIT:{{WRKDIR}}/LICENSE_MIT
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		devel/cl-asdf

do-build:
	(cd ${WRKSRC} && /bin/sh make-asdf.sh build_asdf)

post-extract:
	${AWK} '/LICENSE START/,/LICENSE END/' \
		${WRKSRC}/header.lisp > ${WRKDIR}/LICENSE_MIT

do-install:
	@${MKDIR} \
		${STAGEDIR}${PREFIX}/lib/common-lisp/asdf \
		${STAGEDIR}${PREFIX}/lib/common-lisp/contrib \
		${STAGEDIR}${PREFIX}/lib/common-lisp/uiop
	${INSTALL_DATA} ${WRKSRC}/*.lisp ${STAGEDIR}${PREFIX}/lib/common-lisp/asdf
	${INSTALL_DATA} ${WRKSRC}/build/asdf.lisp ${STAGEDIR}${PREFIX}/lib/common-lisp/asdf
	${INSTALL_DATA} ${WRKSRC}/contrib/*.lisp ${STAGEDIR}${PREFIX}/lib/common-lisp/contrib
	${INSTALL_DATA} ${WRKSRC}/uiop/*.lisp ${STAGEDIR}${PREFIX}/lib/common-lisp/uiop
	${INSTALL_DATA} ${WRKSRC}/asdf.asd ${STAGEDIR}${PREFIX}/lib/common-lisp/asdf
	${INSTALL_DATA} ${WRKSRC}/uiop/uiop.asd ${STAGEDIR}${PREFIX}/lib/common-lisp/uiop
	# documentation
	@${MKDIR} ${STAGEDIR}${STD_DOCDIR}
	(cd ${WRKSRC}/doc && ${COPYTREE_SHARE} . ${STAGEDIR}${STD_DOCDIR})
	(cd ${STAGEDIR}${STD_DOCDIR} && ${RM} .htaccess Changelog Makefile)

[FILE:622:descriptions/desc.primary]
ASDF is the de facto standard build facility for Common Lisp. Your Lisp 
implementation probably contains a copy of ASDF, which you can load using 
(require "asdf").

ASDF 3 is the current successor to Daniel Barlow's ASDF. It was rewritten 
for improved portability, robustness, usability, extensibility, 
configurability, internal consistency, and the ability to deliver 
standalone executables.

ASDF is what Common Lisp hackers use to build and load software. It is the 
successor of the Lisp DEFSYSTEM of yore. ASDF stands for Another System 
Definition Facility.  ASDF 3 contains two parts: asdf/defsystem and uiop.


[FILE:96:distinfo]
fe4edc98dd32999520a4fe0af8859f86af394e4cdf3e34d1e10754815f67d71e       506731 asdf-3.3.4.tar.gz


[FILE:899:manifests/plist.primary]
lib/common-lisp/asdf/
 action.lisp
 asdf.asd
 asdf.lisp
 backward-interface.lisp
 backward-internals.lisp
 bundle.lisp
 component.lisp
 concatenate-source.lisp
 find-component.lisp
 find-system.lisp
 footer.lisp
 forcing.lisp
 header.lisp
 interface.lisp
 lisp-action.lisp
 operate.lisp
 operation.lisp
 output-translations.lisp
 package-inferred-system.lisp
 parse-defsystem.lisp
 plan.lisp
 session.lisp
 source-registry.lisp
 system-registry.lisp
 system.lisp
 upgrade.lisp
 user.lisp
lib/common-lisp/contrib/
 debug-plan.lisp
 debug.lisp
 detect-multiply-used-files.lisp
 fasl-op.lisp
 fast-compile-op.lisp
 wild-modules.lisp
lib/common-lisp/uiop/
 backward-driver.lisp
 common-lisp.lisp
 configuration.lisp
 driver.lisp
 filesystem.lisp
 image.lisp
 launch-program.lisp
 lisp-build.lisp
 os.lisp
 package.lisp
 pathname.lisp
 run-program.lisp
 stream.lisp
 uiop.asd
 utility.lisp
 version.lisp

