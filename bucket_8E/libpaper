# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		libpaper
VERSION=		2.0.1
KEYWORDS=		print
VARIANTS=		standard
SDESC[standard]=	Library for paper size management
HOMEPAGE=		https://github.com/rrthomas/libpaper
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		https://github.com/rrthomas/libpaper/releases/download/v2.0.1/
DISTFILE[1]=		libpaper-2.0.1.tar.gz:main
DF_INDEX=		1
SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILD_DEPENDS=		help2man:single:standard

USES=			autoreconf libtool gmake

DISTNAME=		libpaper-2.0.1

LICENSE=		LGPL3+:single
LICENSE_TERMS=		single:{{WRKDIR}}/TERMS
LICENSE_FILE=		LGPL3+:{{WRKSRC}}/COPYING
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		print/libpaper

MUST_CONFIGURE=		gnu

INSTALL_TARGET=		install-strip
SOVERSION=		2.0.1

post-install:
	(cd ${WRKDIR} && \
		${INSTALL_DATA} papersize.a4 papersize.letter \
		${STAGEDIR}${PREFIX}/etc)
	# only a single package
	${RM} -r ${STAGEDIR}${PREFIX}/share/doc/libpaper

post-configure:
	${REINPLACE_CMD} -e 's|$${prefix}|${PREFIX}|g' \
		${WRKSRC}/config.h ${WRKSRC}/src/paperconfig

post-extract:
	${ECHO_CMD} a4 > ${WRKDIR}/papersize.a4
	${ECHO_CMD} letter > ${WRKDIR}/papersize.letter
	# extract terms
	${AWK} '/^Libpaper is distrib/,/is in the public domain./' \
		${WRKSRC}/README > ${WRKDIR}/TERMS

[FILE:381:descriptions/desc.single]
The paper library and accompanying files are intended to provide a simple
way for applications to take actions based on a system- or user-specified
paper size.

This release is quite minimal, its purpose being to provide really basic
functions (obtaining the system paper name and getting the height and
width of a given kind of paper) that applications can immediately
integrate.


[FILE:100:distinfo]
be69bc746779be8d6c63e4c3b507900bce6e18f3527ce7039269eb57acbf4176      1229250 libpaper-2.0.1.tar.gz


[FILE:263:manifests/plist.single]
bin/paper
etc/
 papersize.a4
 papersize.letter
 paperspecs
include/paper.h
lib/
 libpaper.a
 libpaper.so
 libpaper.so.%%SOMAJOR%%
 libpaper.so.%%SOVERSION%%
sbin/paperconfig
share/man/man1/paper.1.gz
share/man/man5/paperspecs.5.gz
share/man/man8/paperconfig.8.gz

