# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		lmdb
VERSION=		1.0.1
REVISION=		1
KEYWORDS=		databases
VARIANTS=		std
SDESC[std]=		Lightning Memory-Mapped Database
HOMEPAGE=		https://github.com/LMDB/lmdb
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		https://git.openldap.org/openldap/openldap/-/archive/LMDB_1.0.1/
DISTFILE[1]=		openldap-LMDB_1.0.1.tar.bz2:main
DF_INDEX=		1
SPKGS[std]=		set
			primary
			dev
			man

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

USES=			gmake

DISTNAME=		openldap-LMDB_1.0.1/libraries/liblmdb

LICENSE=		CUSTOM1:primary
LICENSE_NAME=		CUSTOM1:"OpenLDAP Public License"
LICENSE_FILE=		CUSTOM1:{{WRKSRC}}/LICENSE
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		databases/lmdb

MAKE_ARGS=		CC="{{CC}}"
			prefix="{{PREFIX}}"
			INSTALL_PROGRAM="{{INSTALL_PROGRAM}}"
			INSTALL_LIB="{{INSTALL_LIB}}"
			INSTALL_DATA="{{INSTALL_DATA}}"
			INSTALL_MAN="{{INSTALL_MAN}}"

SOVERSION=		1.0

VAR_OPSYS[sunos]=	CFLAGS=-DMDB_FDATASYNC=fsync
VAR_OPSYS[linux]=	MAKE_ARGS=LDL=-ldl

post-install:
	${LN} -sf liblmdb.so.1 ${STAGEDIR}${PREFIX}/lib/liblmdb.so

[FILE:498:descriptions/desc.primary]
LMDB is an ultra-fast, ultra-compact key-value data store developed by 
Symas for the OpenLDAP Project.

It uses memory-mapped files, so it has the read performance of a pure 
in-memory database while still offering the persistence of standard 
disk-based databases, and is only limited to the size of the virtual 
address space, (it is not limited to the size of physical RAM). LMDB was 
originally called MDB, but was renamed to avoid confusion with other 
software associated with the name MDB.


[FILE:106:distinfo]
1ae17f11ebdeb0d69e53416bb6e0a7479a7d3d5b5ca443a474bff5b5f886a348       143920 openldap-LMDB_1.0.1.tar.bz2


[FILE:110:manifests/plist.primary]
bin/
 mdb_copy
 mdb_drop
 mdb_dump
 mdb_load
 mdb_stat
lib/
 liblmdb.so.%%SOMAJOR%%
 liblmdb.so.%%SOVERSION%%


[FILE:43:manifests/plist.dev]
include/lmdb.h
lib/
 liblmdb.a
 liblmdb.so


[FILE:76:manifests/plist.man]
share/man/man1/
 mdb_copy.1
 mdb_drop.1
 mdb_dump.1
 mdb_load.1
 mdb_stat.1


[FILE:3208:patches/patch-Makefile]
--- Makefile.orig	2026-08-06 17:11:09 UTC
+++ Makefile
@@ -23,12 +23,12 @@ AR	= ar
 W	= -W -Wall -Wno-unused-parameter -Wbad-function-cast -Wuninitialized
 THREADS = -pthread
 OPT = -O2 -g
-CFLAGS	= $(THREADS) $(OPT) $(W) $(XCFLAGS)
+CFLAGS	+= $(THREADS) $(W)
 LDFLAGS = $(THREADS)
 LDLIBS	= 
 SOLIBS	= 
 SOEXT	= .so
-LDL		= -ldl
+LDL		?=
 
 LIBVER	= 1
 ABIVER	= 0
@@ -36,7 +36,7 @@ VEREXT	= $(LIBVER).$(ABIVER)
 LMDB_VERSION	= 1.0.0
 SOFULL	= $(SOEXT).$(VEREXT)
 
-prefix	= /usr/local
+prefix	?= /usr/local
 exec_prefix = $(prefix)
 bindir = $(exec_prefix)/bin
 libdir = $(exec_prefix)/lib
@@ -67,15 +67,15 @@ install: $(ILIBS) $(IPROGS) $(IHDRS)
 	mkdir -p $(DESTDIR)$(libdir)
 	mkdir -p $(DESTDIR)$(includedir)
 	mkdir -p $(DESTDIR)$(mandir)/man1
-	for f in $(IPROGS); do cp $$f $(DESTDIR)$(bindir); done
-	for f in $(ILIBS); do cp $$f $(DESTDIR)$(libdir); done
-	for f in $(ILIBS2); do cp $$f $(DESTDIR)$(libdir); \
+	for f in $(IPROGS); do ${INSTALL_PROGRAM} $$f $(DESTDIR)$(bindir); done
+	for f in $(ILIBS); do $(INSTALL_LIB) $$f $(DESTDIR)$(libdir); done
+	for f in $(ILIBS2); do ${INSTALL_LIB} $$f $(DESTDIR)$(libdir); \
 		i=`basename -s .$(ABIVER) $$f`; rm -f $(DESTDIR)$(libdir)/$$i; \
 		ln -s $$f $(DESTDIR)$(libdir)/$$i; \
 		i=`basename -s .$(LIBVER) $$i`; rm -f $(DESTDIR)$(libdir)/$$i; \
 		ln -s $$f $(DESTDIR)$(libdir)/$$i; done
-	for f in $(IHDRS); do cp $$f $(DESTDIR)$(includedir); done
-	for f in $(IDOCS); do cp $$f $(DESTDIR)$(mandir)/man1; done
+	for f in $(IHDRS); do ${INSTALL_DATA} $$f $(DESTDIR)$(includedir); done
+	for f in $(IDOCS); do ${INSTALL_MAN} $$f $(DESTDIR)$(mandir)/man1; done
 
 clean:
 	rm -rf $(PROGS) $(RPROGS) *.[ao] *.[ls]o *.so.* *~ testdb
@@ -89,7 +89,7 @@ liblmdb.a:	mdb.o midl.o module.o
 
 liblmdb$(SOFULL):	mdb.lo midl.lo module.lo
 #	$(CC) $(LDFLAGS) -pthread -shared -Wl,-Bsymbolic -o $@ mdb.o midl.o $(SOLIBS)
-	$(CC) $(LDFLAGS) -shared $(VERSION_OPT) -o $@ mdb.lo midl.lo module.lo $(SOLIBS) $(LDL)
+	$(CC) $(LDFLAGS) -shared $(VERSION_OPT) -Wl,-soname,$@ -o $@ mdb.lo midl.lo module.lo $(SOLIBS) $(LDL)
 	rm -f liblmdb$(SOEXT); ln -s $@ liblmdb$(SOEXT)
 	rm -f $(SOVER); ln -s $@ $(SOVER)
 
@@ -104,13 +104,21 @@ mdb_load: mdb_load.o liblmdb.a
 mdb_drop: mdb_drop.o liblmdb.a
 	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest:    mtest.o    liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest2:	mtest2.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest3:	mtest3.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest4:	mtest4.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest5:	mtest5.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest6:	mtest6.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest_remap:  mtest_remap.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest_enc:    mtest_enc.o chacha8.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 mtest_enc2:	  mtest_enc2.o liblmdb.a crypto.lm
 	$(CC) $(LDFLAGS) -pthread -o $@ $@.o liblmdb.a $(LDL)
 mtest_enc3:	  mtest_enc3.o liblmdb.a crypto.lm
@@ -119,6 +127,7 @@ mtest_enc4:	  mtest_enc4.o liblmdb.a cry
 	$(CC) $(LDFLAGS) -pthread -o $@ $@.o liblmdb.a $(LDL)
 
 mplay:	mplay.o liblmdb.a
+	$(CC) $(LDFLAGS) -o $@ $^ $(LDL)
 
 crypto.lm:	crypto.c
 	$(CC) -shared $(CFLAGS) -o $@ $^ -lsodium


[FILE:342:patches/patch-mdb.c]
--- mdb.c.orig	2026-08-06 17:11:09 UTC
+++ mdb.c
@@ -197,6 +197,8 @@ typedef SSIZE_T	ssize_t;
 #elif defined(__HAIKU__)
 # define MDB_USE_POSIX_SEM	1
 # define MDB_FDATASYNC		fsync
+#elif defined(__FreeBSD_version) && __FreeBSD_version < 1101000
+# define MDB_FDATASYNC		fsync
 #endif
 
 /* NetBSD does not define union semun in sys/sem.h */

