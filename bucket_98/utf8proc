# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		utf8proc
VERSION=		2.4.0.1
KEYWORDS=		textproc devel
VARIANTS=		standard
SDESC[standard]=	UTF-8 processing library
HOMEPAGE=		https://github.com/JuliaStrings/utf8proc
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		https://download.netsurf-browser.org/libs/releases/
DISTFILE[1]=		libutf8proc-2.4.0-1-src.tar.gz:main
DF_INDEX=		1
SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

USES=			cmake

DISTNAME=		libutf8proc-2.4.0-1

LICENSE=		MIT:single
LICENSE_FILE=		MIT:{{WRKSRC}}/LICENSE.md
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		textproc/utf8proc

INVALID_RPATH=		yes

SOVERSION=		2.3.1

CMAKE_ARGS=		-DBUILD_SHARED_LIBS:BOOL=ON

post-install:
	${STRIP_CMD} ${STAGEDIR}${PREFIX}/lib/libutf8proc.${LIBEXT}
	${SED}  -e 's#PREFIX#${PREFIX}#' \
		-e 's#LIBDIR#lib#' \
		-e 's#INCLUDEDIR#include#' \
		-e 's#VERSION#2.3.1#' \
		${WRKSRC}/libutf8proc.pc.in \
		> ${STAGEDIR}${PREFIX}/lib/pkgconfig/libutf8proc.pc

post-extract:
	${MV} ${WRKSRC}/src/utf* ${WRKSRC}/
	${MV} ${WRKSRC}/include/libutf8proc/utf* ${WRKSRC}/

[FILE:439:descriptions/desc.single]
utf8proc is a library for processing UTF-8 encoded Unicode strings.
Some features are Unicode normalization, stripping of default ignorable
characters, case folding and detection of grapheme cluster boundaries.
A special character mapping is available, which converts for example the
characters "Hyphen" (U+2010), "Minus" (U+2212) and
"Hyphen-Minus" (U+002D, ASCII Minus) all into the ASCII minus sign, to
make them equal for comparisons.


[FILE:109:distinfo]
01ab1d6989c1c7755090db24c3f64e49f95c560ae49c26bec5142bac6802c472       154746 libutf8proc-2.4.0-1-src.tar.gz


[FILE:148:manifests/plist.single]
%%EXCLUDE-DARWIN%%lib/libutf8proc.so.%%SOVERSION%%
include/utf8proc.h
lib/
 libutf8proc.so
 libutf8proc.so.%%SOMAJOR%%
lib/pkgconfig/libutf8proc.pc

