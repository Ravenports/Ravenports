# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		R-fs
VERSION=		2.0.1
KEYWORDS=		cran
VARIANTS=		std
SDESC[std]=		Cross-platform File system operations
HOMEPAGE=		https://fs.r-lib.org
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
			https://loki.dragonflybsd.org/cranfiles/
DISTFILE[1]=		fs_2.0.1.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILD_DEPENDS=		libuv:dev:std
BUILDRUN_DEPENDS=	libuv:primary:std

USES=			cran gmake iconv:build zlib:build xz:build bz2:build
			pcre2:build zstd:build

DISTNAME=		fs

GENERATED=		yes

INSTALL_REQ_TOOLCHAIN=	yes

[FILE:153:descriptions/desc.single]
fs: Cross-Platform File System Operations Based on 'libuv'

A cross-platform interface to file system operations, built on top of the
'libuv' C library.


[FILE:99:distinfo]
1d1706d748772d16fd1a8b7f667c761948bb931199f85fad081396847c07e0c2      1471271 CRAN/fs_2.0.1.tar.gz


[FILE:612:patches/patch-src_Makevars.in]
--- src/Makevars.in.orig	2026-03-21 21:48:10 UTC
+++ src/Makevars.in
@@ -8,12 +8,20 @@ UNAME := $(shell uname)
 ifeq ($(UNAME), FreeBSD)
 PKG_LIBS += -lkvm
 endif
+ifeq ($(UNAME), MidnightBSD)
+PKG_LIBS += -lkvm
+endif
 ifeq ($(UNAME), OpenBSD)
 PKG_LIBS += -lkvm
 endif
 ifeq ($(UNAME), NetBSD)
 PKG_LIBS += -lkvm
 endif
+ifeq ($(UNAME), SunOS)
+PKG_LIBS += -lkstat -lsendfile -lsocket -lxnet
+OBJECTS +=  bsd/setmode.o bsd/strmode.o bsd/reallocarray.o
+CONFTGT = --build=i86-pc-solaris2.10
+endif
 ifneq ($(filter $(UNAME), Linux AIX OS400),)
 OBJECTS +=  bsd/setmode.o bsd/strmode.o bsd/reallocarray.o
 endif


[FILE:367:patches/patch-src_unix_getmode.cc]
--- src/unix/getmode.cc.orig	2026-03-03 17:53:42 UTC
+++ src/unix/getmode.cc
@@ -2,6 +2,7 @@
 #include "utils.h"
 
 #if (defined(__APPLE__) && defined(__MACH__)) || defined(__OpenBSD__) ||       \
+    defined(__DragonFly__) || \
     defined(__FreeBSD__) || defined(__NetBSD__)
 #include <string.h> /* for strmode */
 #include <unistd.h> /* for getmode / setmode */

