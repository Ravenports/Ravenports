# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		R-splitfngr
VERSION=		0.1.2
KEYWORDS=		cran
VARIANTS=		std
SDESC[std]=		Combined Evaluation and Split Access of Functions
HOMEPAGE=		none
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
			https://loki.dragonflybsd.org/cranfiles/
DISTFILE[1]=		splitfngr_0.1.2.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILDRUN_DEPENDS=	R-lbfgs:single:std

USES=			cran gmake

DISTNAME=		splitfngr

GENERATED=		yes

INSTALL_REQ_TOOLCHAIN=	yes

[FILE:635:descriptions/desc.single]
splitfngr: Combined Evaluation and Split Access of Functions

Some R functions, such as optim(), require a function its gradient passed
as separate arguments. When these are expensive to calculate it may be much
faster to calculate the function (fn) and gradient (gr) together since they
often share many calculations (chain rule). This package allows the user to
pass in a single function that returns both the function and gradient, then
splits (hence 'splitfngr') them so the results can be accessed separately.
The functions provided allow this to be done with any number of
functions/values, not just for functions and gradients.


[FILE:106:distinfo]
9a225e29c2c646e0008a489b2c69f322644bd0b1a08a711448488a5391f1c931         4980 CRAN/splitfngr_0.1.2.tar.gz

