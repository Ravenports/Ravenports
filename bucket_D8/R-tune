# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		R-tune
VERSION=		2.0.0
KEYWORDS=		cran
VARIANTS=		std
SDESC[std]=		Tidy Tuning Tools
HOMEPAGE=		https://tune.tidymodels.org/
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
			https://loki.dragonflybsd.org/cranfiles/
DISTFILE[1]=		tune_2.0.0.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILDRUN_DEPENDS=	R-cli:single:std
			R-dials:single:std
			R-dplyr:single:std
			R-generics:single:std
			R-ggplot2:single:std
			R-glue:single:std
			R-GPfit:single:std
			R-hardhat:single:std
			R-parsnip:single:std
			R-purrr:single:std
			R-recipes:single:std
			R-rlang:single:std
			R-rsample:single:std
			R-tailor:single:std
			R-tibble:single:std
			R-tidyr:single:std
			R-tidyselect:single:std
			R-vctrs:single:std
			R-withr:single:std
			R-workflows:single:std
			R-yardstick:single:std

USES=			cran gmake

DISTNAME=		tune

GENERATED=		yes

INSTALL_REQ_TOOLCHAIN=	yes

[FILE:275:descriptions/desc.single]
tune: Tidy Tuning Tools

The ability to tune models is important. 'tune' contains functions and
classes to be used in conjunction with other 'tidymodels' packages for
finding reasonable values of hyper-parameters in models, pre-processing
methods, and post-processing steps.


[FILE:101:distinfo]
58fb40cbf74a397e53884b2d49cddc4e3d92b973b48c8e9c0b64181f54497a14      1275574 CRAN/tune_2.0.0.tar.gz

