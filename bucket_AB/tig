# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		tig
VERSION=		2.6.1
KEYWORDS=		devel
VARIANTS=		std
SDESC[std]=		Text-mode interface for git
HOMEPAGE=		https://jonas.github.io/tig/
CONTACT=		Michael_Neumann[mneumann@ntecs.de]

DOWNLOAD_GROUPS=	main
SITES[main]=		https://github.com/jonas/tig/releases/download/tig-2.6.1/
DISTFILE[1]=		tig-2.6.1.tar.gz:main
DF_INDEX=		1
SPKGS[std]=		set
			primary
			man

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

RUN_DEPENDS=		git:primary:std

USES=			iconv gmake ncurses readline pkgconfig

LICENSE=		GPLv2+:primary
LICENSE_TERMS=		primary:{{WRKDIR}}/TERMS
LICENSE_FILE=		GPLv2+:{{WRKSRC}}/COPYING
LICENSE_AWK=		TERMS:"^$$"
LICENSE_SOURCE=		TERMS:{{WRKSRC}}/src/tig.c
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		devel/tig

MUST_CONFIGURE=		gnu
CONFIGURE_ARGS=		{{ICONV_CONFIGURE_BASE}}

BUILD_TARGET=		all strip doc-man
MAKE_ARGS=		V=1

INSTALL_TARGET=		install install-doc-man

CFLAGS=			-I{{NCURSESINC}}

[FILE:224:descriptions/desc.primary]
Tig is an ncurses-based text-mode interface for git. It functions mainly as
a Git repository browser, but can also assist in staging changes for commit
at chunk level and act as a pager for output from various Git commands.


[FILE:95:distinfo]
5adeabdcd93aa0423d618da8b878b53482bef6e0e9e1fe224acc0f18031fe91e      1200780 tig-2.6.1.tar.gz


[FILE:18:manifests/plist.primary]
bin/tig
etc/tigrc


[FILE:71:manifests/plist.man]
share/man/man1/tig.1
share/man/man5/tigrc.5
share/man/man7/tigmanual.7


[FILE:308:patches/patch-src_stage.c]
--- src/stage.c.orig	2026-06-13 17:25:29 UTC
+++ src/stage.c
@@ -347,7 +347,7 @@ stage_insert_chunk(struct view *view, st
 		const char *text = box_text(line);
 
 		if (!add_line_text_at(view, after_lineno++, text, line->type, 1))
-			return false;
+			return NULL;
 	}
 
 	return view->line + after_lineno;


[FILE:544:patches/patch-src_ui.c]
--- src/ui.c.orig	2026-06-13 17:25:29 UTC
+++ src/ui.c
@@ -285,7 +285,7 @@ open_file_finder(const char *commit)
 
 	if (!file_finder_read(&finder, commit)) {
 		file_finder_done(&finder);
-		return false;
+		return NULL;
 	}
 
 	getmaxyx(stdscr, finder.height, finder.width);
@@ -293,7 +293,7 @@ open_file_finder(const char *commit)
 	finder.win = newwin(finder.height, finder.width, 0, 0);
 	if (!finder.win) {
 		file_finder_done(&finder);
-		return false;
+		return NULL;
 	}
 
 	finder.keymap = get_keymap("search", STRING_SIZE("search"));

