# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		dasm
VERSION=		2.20.17
KEYWORDS=		devel lang
VARIANTS=		std
SDESC[std]=		Versatile macro assembler for 8bit microprocessors
HOMEPAGE=		https://dasm-assembler.github.io
CONTACT=		Todd_Martin[warfox@sdf.org]

DOWNLOAD_GROUPS=	main
SITES[main]=		GITHUB/dasm-assembler:dasm:v2.20.17
DISTFILE[1]=		generated:main
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

USES=			gmake

LICENSE=		GPLv2:single
LICENSE_TERMS=		single:{{WRKDIR}}/TERMS
LICENSE_FILE=		GPLv2:{{WRKSRC}}/LICENSE
LICENSE_AWK=		TERMS:"^$$"
LICENSE_SOURCE=		TERMS:{{WRKSRC}}/src/main.c
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		devel/dasm

post-install:
	${STRIP_CMD} ${STAGEDIR}${PREFIX}/bin/*

do-install:
	${INSTALL_PROGRAM} ${WRKSRC}/src/dasm ${STAGEDIR}${PREFIX}/bin
	${INSTALL_PROGRAM} ${WRKSRC}/src/ftohex ${STAGEDIR}${PREFIX}/bin

[FILE:59:descriptions/desc.single]
Dasm, a macro assembler for various 8-bit microprocessors.


[FILE:113:distinfo]
4755532fe8c990c8616b4cfbe22c3fe5820e40476343da01e088b617bd2d1144      1501712 dasm-assembler-dasm-2.20.17.tar.gz


[FILE:19:manifests/plist.single]
bin/
 dasm
 ftohex

