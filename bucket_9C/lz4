# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		lz4
VERSION=		1.8.0
KEYWORDS=		archivers
VARIANTS=		standard
SDESC[standard]=	LZ4 compression library, lossless and very fast
HOMEPAGE=		http://www.lz4.org/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		GITHUB/lz4:lz4:v1.8.0
DISTFILE[1]=		generated:main
DF_INDEX=		1
SPKGS[standard]=	complete
			static
			shared

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

USES=			gmake pkgconfig

LICENSE=		BSD2CLAUSE:static GPLv2:static
LICENSE_SCHEME=		multi
LICENSE_FILE=		BSD2CLAUSE:{{WRKSRC}}/LICENSE
			GPLv2:{{WRKSRC}}/LICENSE

FPC_EQUIVALENT=		archivers/liblz4

BUILD_TARGET=		default

INSTALL_REQ_TOOLCHAIN=	yes
PLIST_SUB=		SO_VER=1.8.0
			SO_VER_MAJ=1

post-patch:
	@${REINPLACE_CMD} -e '/^all:/s/$$/ liblz4.pc/' \
		${WRKSRC}/lib/Makefile

post-install:
	@${STRIP_CMD} ${STAGEDIR}${PREFIX}/bin/lz4*
	@${STRIP_CMD} ${STAGEDIR}${PREFIX}/lib/liblz4.so.1.8.0

[FILE:435:descriptions/desc.static]
LZ4 is a very fast lossless compression algorithm, providing compression
speed at 400 MB/s per core, scalable with multi-cores CPU. It also features
an extremely fast decoder, with speed in multiple GB/s per core, typically
reaching RAM speed limits on multi-core systems.

A high compression derivative, called LZ4_HC, is also provided. It trades
CPU time for compression ratio.

This package contains the static library and headers.


[FILE:441:descriptions/desc.shared]
LZ4 is a very fast lossless compression algorithm, providing compression
speed at 400 MB/s per core, scalable with multi-cores CPU. It also features
an extremely fast decoder, with speed in multiple GB/s per core, typically
reaching RAM speed limits on multi-core systems.

A high compression derivative, called LZ4_HC, is also provided. It trades
CPU time for compression ratio.

This package contains the shared libraries and executables.


[FILE:99:distinfo]
2ca482ea7a9bb103603108b5a7510b7592b90158c151ff50a28f1ca8389fccf6       222840 lz4-lz4-1.8.0.tar.gz


[FILE:93:manifests/plist.static]
include/
 lz4.h
 lz4frame.h
 lz4frame_static.h
 lz4hc.h
lib/liblz4.a
lib/pkgconfig/liblz4.pc


[FILE:157:manifests/plist.shared]
bin/
 lz4
 lz4c
 lz4cat
 unlz4
lib/
 liblz4.so
 liblz4.so.%%SO_VER_MAJ%%
 liblz4.so.%%SO_VER%%
share/man/man1/
 lz4.1.gz
 lz4c.1.gz
 lz4cat.1.gz
 unlz4.1.gz


[FILE:402:patches/patch-lib_Makefile]
--- lib/Makefile.orig	2017-08-17 17:55:30 UTC
+++ lib/Makefile
@@ -126,11 +126,7 @@ LIBDIR      ?= $(libdir)
 includedir  ?= $(PREFIX)/include
 INCLUDEDIR  ?= $(includedir)
 
-ifneq (,$(filter $(shell uname),OpenBSD FreeBSD NetBSD DragonFly))
-PKGCONFIGDIR ?= $(PREFIX)/libdata/pkgconfig
-else
 PKGCONFIGDIR ?= $(LIBDIR)/pkgconfig
-endif
 
 ifneq (,$(filter $(shell uname),SunOS))
 INSTALL ?= ginstall


[FILE:443:patches/patch-programs_Makefile]
--- programs/Makefile.orig	2017-08-17 17:55:30 UTC
+++ programs/Makefile
@@ -134,11 +134,7 @@ BINDIR      ?= $(bindir)
 datarootdir ?= $(PREFIX)/share
 mandir      ?= $(datarootdir)/man
 
-ifneq (,$(filter $(shell uname),OpenBSD FreeBSD NetBSD DragonFly SunOS))
-MANDIR  ?= $(PREFIX)/man/man1
-else
-MANDIR  ?= $(mandir)
-endif
+MANDIR  ?= $(PREFIX)/share/man/man1
 
 INSTALL_PROGRAM ?= $(INSTALL) -m 755
 INSTALL_DATA    ?= $(INSTALL) -m 644

