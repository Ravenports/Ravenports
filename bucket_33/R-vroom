# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		R-vroom
VERSION=		1.7.0
KEYWORDS=		cran
VARIANTS=		std
SDESC[std]=		Read and Write Rectangular Text Data Quickly
HOMEPAGE=		https://vroom.tidyverse.org
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
			https://loki.dragonflybsd.org/cranfiles/
DISTFILE[1]=		vroom_1.7.0.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILDRUN_DEPENDS=	R-bit64:single:std
			R-cli:single:std
			R-crayon:single:std
			R-glue:single:std
			R-hms:single:std
			R-lifecycle:single:std
			R-rlang:single:std
			R-tibble:single:std
			R-tidyselect:single:std
			R-tzdb:single:std
			R-vctrs:single:std
			R-withr:single:std
			R-cpp11:single:std
			R-progress:single:std

USES=			cran gmake shebangfix

DISTNAME=		vroom

SHEBANG_FILES=		inst/bench/script.sh
			inst/bench/download-data.sh
GENERATED=		yes

INSTALL_REQ_TOOLCHAIN=	yes

[FILE:364:descriptions/desc.single]
vroom: Read and Write Rectangular Text Data Quickly

The goal of 'vroom' is to read and write data (like 'csv', 'tsv' and 'fwf')
quickly. When reading it uses a quick initial indexing step, then reads the
values lazily , so only the data you actually use needs to be read. The
writer formats the data in parallel and writes to disk asynchronously from
formatting.


[FILE:102:distinfo]
6749c24ff27342a807c5816a0d773d9163febbde0c0247469ef45dae9d7950fc       619356 CRAN/vroom_1.7.0.tar.gz

