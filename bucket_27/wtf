# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		wtf
VERSION=		20230906
REVISION=		1
KEYWORDS=		games
VARIANTS=		std
SDESC[std]=		Translates common Internet acronyms
HOMEPAGE=		https://sourceforge.net/projects/bsdwtf/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		SF/bsdwtf
DISTFILE[1]=		wtf-20230906.tar.gz:main
DF_INDEX=		1
SPKGS[std]=		set
			primary
			man

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		PUBDOM:primary
LICENSE_FILE=		PUBDOM:{{WRKDIR}}/LICENSE
LICENSE_AWK=		PUBDOM:"^$$"
LICENSE_SOURCE=		PUBDOM:{{WRKSRC}}/wtf
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		games/wtf

SKIP_BUILD=		yes

post-extract:
	${REINPLACE_CMD} -e 's|/usr/share/misc|${PREFIX}/share/wtf|g' ${WRKSRC}/wtf*

do-install:
	${INSTALL_SCRIPT} ${WRKSRC}/wtf ${STAGEDIR}${PREFIX}/bin
	${INSTALL_MAN} ${WRKSRC}/wtf.6 ${STAGEDIR}${MAN6PREFIX}/man/man6
	${MKDIR} ${STAGEDIR}${PREFIX}/share/wtf
	${INSTALL_DATA} ${WRKSRC}/acronyms ${STAGEDIR}${PREFIX}/share/wtf
	${INSTALL_DATA} ${WRKSRC}/acronyms.comp ${STAGEDIR}${PREFIX}/share/wtf
	${INSTALL_DATA} ${WRKSRC}/acronyms-o.real ${STAGEDIR}${PREFIX}/share/wtf/acronyms-o

[FILE:185:descriptions/desc.primary]
The wtf utility displays the expansion of the acronyms
specified on the command line. If the acronym is unknown,
wtf will check to see if the acronym is known by the
whatis(1) command.


[FILE:98:distinfo]
ed9c1fa927fcd878cce955fb0bdc586876ee1ae234666be75c3bbd6e5b2a094b        31564 wtf-20230906.tar.gz


[FILE:56:manifests/plist.primary]
bin/wtf
share/wtf/
 acronyms
 acronyms-o
 acronyms.comp


[FILE:21:manifests/plist.man]
share/man/man6/wtf.6

