# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		php85-pcntl
VERSION=		8.5.1
KEYWORDS=		lang www php devel
VARIANTS=		std
SDESC[std]=		PHP 8.5 extension: pcntl (process control)
HOMEPAGE=		https://php.net/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		PHP/distributions
DISTFILE[1]=		php-8.5.1.tar.xz:main
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

USES=			php:ext,85 cpe

DISTNAME=		php-8.5.1/ext/{{PHP_MODNAME}}
EXTRACT_HEAD[1]=	{{TAR}} -xf
EXTRACT_TAIL[1]=	php-8.5.1/ext/{{PHP_MODNAME}}

CPE_PRODUCT=		php
CPE_VENDOR=		php
FPC_EQUIVALENT=		devel/php85-pcntl

CONFIGURE_ARGS=		--enable-pcntl

CVE_FIXED=		CVE-2007-3205

[FILE:55:descriptions/desc.single]
This package contains the pcntl extension for PHP 8.4.


[FILE:95:distinfo]
3f5bf99ce81201f526d25e288eddb2cfa111d068950d1e9a869530054ff98815     14326700 php-8.5.1.tar.xz


[FILE:566:patches/patch-pcntl.c]
--- pcntl.c.orig	2025-11-18 08:02:20 UTC
+++ pcntl.c
@@ -437,8 +437,13 @@ PHP_FUNCTION(pcntl_waitid)
 		}
 		struct rusage rusage;
 # if defined(HAVE_WAIT6) /* FreeBSD */
+#    if defined(__NetBSD__)
+		struct wrusage wrusage;
+		memset(&wrusage, 0, sizeof(struct wrusage));
+#    else
 		struct __wrusage wrusage;
 		memset(&wrusage, 0, sizeof(struct __wrusage));
+#    endif
 		pid_t pid = wait6((idtype_t) idtype, (id_t) id, &status, (int) options, &wrusage, &siginfo);
 		status = pid > 0 ? 0 : pid;
 		memcpy(&rusage, &wrusage.wru_self, sizeof(struct rusage));

