# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		R-RCurl
VERSION=		1.98-1.16
KEYWORDS=		cran
VARIANTS=		std
SDESC[std]=		General network (HTTP/FTP/...) client interface
HOMEPAGE=		none
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
			https://loki.dragonflybsd.org/cranfiles/
DISTFILE[1]=		RCurl_1.98-1.16.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILDRUN_DEPENDS=	R-bitops:single:std

USES=			cran gmake iconv curl
GNOME_COMPONENTS=	libxml2

DISTNAME=		RCurl

GENERATED=		yes

INSTALL_REQ_TOOLCHAIN=	yes

[FILE:765:descriptions/desc.single]
RCurl: General Network (HTTP/FTP/...) Client Interface for R

A wrapper for 'libcurl' &lt;<a href="https://curl.se/libcurl/"
target="_top">https://curl.se/libcurl/</a>&gt; Provides functions to allow
one to compose general HTTP requests and provides convenient functions to
fetch URIs, get &amp; post forms, etc. and process the results returned by
the Web server. This provides a great deal of control over the HTTP/FTP/...
connection and the form of the request while providing a higher-level
interface than is available just using R socket connections. Additionally,
the underlying implementation is robust and extensive, supporting
FTP/FTPS/TFTP (uploads and downloads), SSL/HTTPS, telnet, dict, ldap, and
also supports cookies, redirects, authentication, etc.


[FILE:106:distinfo]
1a8dc40e10593617348071c6265cc7fac22e26271564206b308a3badfc6c0da7       728943 CRAN/RCurl_1.98-1.16.tar.gz

