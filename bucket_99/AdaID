# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		AdaID
VERSION=		1.0.3
KEYWORDS=		devel ada
VARIANTS=		standard
SDESC[standard]=	Simple Ada library for generating UUIDs
HOMEPAGE=		https://github.com/anthony-arnold/AdaID
CONTACT=		John_Marino[draco@marino.st]

DOWNLOAD_GROUPS=	main
SITES[main]=		GITHUB/anthony-arnold:AdaID:1.0.3
DISTFILE[1]=		generated:main
DF_INDEX=		1
SPKGS[standard]=	complete
			primary
			dev

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILD_DEPENDS=		aunit:primary:standard

USES=			gprbuild gmake ada:primary

LICENSE=		BSD2CLAUSE:primary
LICENSE_FILE=		BSD2CLAUSE:{{WRKSRC}}/LICENSE
LICENSE_SCHEME=		solo

INSTALL_REQ_TOOLCHAIN=	yes
SOVERSION=		1.0.3

post-install:
	${STRIP_CMD} ${STAGEDIR}${PREFIX}/lib/libadaid.so

do-install:
	${SETENV} ${MAKE_ENV} gprinstall\
		--prefix=${STAGEDIR}${PREFIX}\
		--lib-subdir=lib/adaid\
		--sources-subdir=include/adaid\
		--link-lib-subdir=lib \
		--no-manifests\
		-p -P ${WRKSRC}/adaid.gpr

[FILE:227:descriptions/desc.primary]
AdaID is a simple Ada library for generating UUIDs.

Supported operations include:
  * Generating random UUIDs
  * Generating name-based UUIDs
  * Restoring UUIDs from a string representation.
  * Converting a UUID to a string


[FILE:112:distinfo]
5d42b60466de393a6bfe9bf02cb2e1899376b78ba2601aad582dcad7bca886d5        14965 anthony-arnold-AdaID-1.0.3.tar.gz


[FILE:66:manifests/plist.primary]
lib/libadaid.so.%%SOVERSION%%
lib/adaid/libadaid.so.%%SOVERSION%%


[FILE:252:manifests/plist.dev]
include/adaid/
 adaid-generate.adb
 adaid-generate.ads
 adaid.adb
 adaid.ads
 sha-process_data.adb
 sha-process_data.ads
 sha.ads
lib/libadaid.so
lib/adaid/
 adaid-generate.ali
 adaid.ali
 libadaid.so
 sha-process_data.ali
 sha.ali
share/gpr/adaid.gpr


[FILE:271:patches/patch-Makefile]
--- Makefile.orig	2021-12-08 13:50:42 UTC
+++ Makefile
@@ -15,7 +15,7 @@ CP=cp
 
 #library
 $(LIB): src/*.adb src/*.ads include/*.ads adaid.gpr
-	$(GNAT) make -Padaid.gpr
+	gprbuild -p -Padaid.gpr
 
 adaid.gpr: adaid.gpr.in
 	@cat $< | sed 's/%VERSION%/$(VERSION)/' > $@


[FILE:274:files/adaid.gpr]
project AdaID is
   for Languages        use ("ada");
   for Library_Name     use "adaid";
   for Source_Dirs      use ("../../include/adaid");
   for Object_Dir       use "../objs";
   for Library_Dir      use "../lib";
   for Library_Kind     use "static-pic";
end AdaID;

