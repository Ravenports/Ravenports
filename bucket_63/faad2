# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		faad2
VERSION=		2.8.8
KEYWORDS=		audio
VARIANTS=		standard
SDESC[standard]=	Freeware Advanced Audio Decoder version 2
HOMEPAGE=		http://www.audiocoding.com/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		SF/faac/faad2-src/faad2-2.8.0
DISTFILE[1]=		faad2-2.8.8.tar.gz:main
DF_INDEX=		1
SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

USES=			autoreconf gmake libtool

FPC_EQUIVALENT=		audio/faad

MUST_CONFIGURE=		gnu

CFLAGS=			-D__GNU_LIBRARY__=1
LDFLAGS=		-lpthread

post-patch:
	${REINPLACE_CMD} -e 's|faad.man|faad.1|g' \
		${WRKSRC}/frontend/Makefile.am
	${REINPLACE_CMD} -e 's|^AM_CFLAGS|AM_CPPFLAGS|g ; \
		 s|-iquote |-I|g' ${WRKSRC}/libfaad/Makefile.am
	${REINPLACE_CMD} -e 's|= -DUSE_|= -fPIC -DPIC -DUSE_|g' \
		${WRKSRC}/common/mp4ff/Makefile.am
	${ECHO_CMD} "SUBDIRS = libfaad common frontend" > \
		${WRKSRC}/Makefile.am

post-install:
	${STRIP_CMD} ${STAGEDIR}${PREFIX}/lib/libfaad.so
	${STRIP_CMD} ${STAGEDIR}${PREFIX}/lib/libfaad_drm.so

post-configure:
	${REINPLACE_CMD} "s/444/644/g" ${WRKSRC}/common/mp4ff/Makefile

post-extract:
	${LN} -sf faad.man ${WRKSRC}/frontend/faad.1

[FILE:266:descriptions/desc.single]
FAAD2 (Freeware Advanced Audio Decoder) is a MPEG-2, MPEG-4 AAC decoder.

FAAD2 is the fastest ISO AAC audio decoder available and can be used with
DRM (Digital Radio Mondiale), and supports HE (High Efficiency), Main,
LC, LD, LTP, PS (Parametric Stereo) AAC files.


[FILE:97:distinfo]
985c3fadb9789d2815e50f4ff714511c79c2710ac27a4aaaf5c0c2662141426d      1069044 faad2-2.8.8.tar.gz


[FILE:194:manifests/plist.single]
bin/faad
include/
 faad.h
 neaacdec.h
lib/
 libfaad_drm.a
 libfaad_drm.so
 libfaad_drm.so.2
 libfaad_drm.so.2.0.0
 libfaad.a
 libfaad.so
 libfaad.so.2
 libfaad.so.2.0.0
share/man/man1/faad.1.gz

