# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		R-caret
VERSION=		7.0-1
KEYWORDS=		cran
VARIANTS=		std
SDESC[std]=		Classification and Regression Training
HOMEPAGE=		https://github.com/topepo/caret/
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
			https://loki.dragonflybsd.org/cranfiles/
DISTFILE[1]=		caret_7.0-1.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1
SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILDRUN_DEPENDS=	R-ggplot2:single:std
			R-e1071:single:std
			R-foreach:single:std
			R-ModelMetrics:single:std
			R-plyr:single:std
			R-pROC:single:std
			R-recipes:single:std
			R-reshape2:single:std
			R-withr:single:std

USES=			cran gmake

DISTNAME=		caret

GENERATED=		yes

INSTALL_REQ_TOOLCHAIN=	yes

[FILE:330:descriptions/desc.single]
The caret package (short for Classification And REgression Training)
is a set of functions that attempt to streamline the process for
creating predictive models. The package contains tools for:

 * data splitting
 * pre-processing
 * model tuning using resampling
 * variable importance estimation

as well as other functionality.

[FILE:102:distinfo]
d1ea51a8ce1948718b424bacda84428bd397d8b7869310a96311553d329a815c      2273919 CRAN/caret_7.0-1.tar.gz

