# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		heme
VERSION=		0.4.2
KEYWORDS=		editors
VARIANTS=		standard
SDESC[standard]=	Fast and portable console hex editor
HOMEPAGE=		http://heme.sourceforge.net/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		SF/heme/heme/heme-0.4.2
DISTFILE[1]=		heme-0.4.2.tar.gz:main
DF_INDEX=		1
SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

USES=			ncurses

FPC_EQUIVALENT=		editors/heme

BUILD_TARGET=		heme
MAKE_ARGS=		CFLAGS="{{CFLAGS}}"
			LDFLAGS="{{LDFLAGS}}"
			INSTALL_PREFIX="{{STAGEDIR}}{{PREFIX}}"

CFLAGS=			-DHAVE_MMAP
			-I{{NCURSESINC}}
LDFLAGS=		-lncurses
VAR_OPSYS[sunos]=	LDFLAGS=-ltinfo

post-patch:
	${REINPLACE_CMD} -e 's|/man/|/share/man/|g' ${WRKSRC}/Makefile

[FILE:549:descriptions/desc.single]
Heme is intended to be fast and portable console hex editor for Unix-like
systems.  It has undo support (number of undo operations is only limited by
available memory), ability to fill a range of addresses with the specified
byte, ability to search for a single byte or character string.  Offsets can
be given in hexadecimal, octal, or decimal forms.  There are two editing
modes: hex (binary) and ASCII (text).

Heme uses standard curses library for screen and input handling, and offers
colors support (they can be set in the configuration file).


[FILE:96:distinfo]
3ba5c7883cb4b79f22d5b9b6f401c70655c41518531441347e5fd15c8bb45973        21815 heme-0.4.2.tar.gz


[FILE:34:manifests/plist.single]
bin/heme
share/man/man1/heme.1.gz

