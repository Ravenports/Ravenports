# Buildsheet autogenerated by ravenadm tool -- Do not edit.

NAMEBASE=		sdl1_image
VERSION=		1.2.12
KEYWORDS=		graphics
VARIANTS=		standard
SDESC[standard]=	Load images of various formats as SDL surfaces
HOMEPAGE=		http://www.libsdl.org/projects/SDL_image/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		http://www.libsdl.org/projects/SDL_image/release/
DISTFILE[1]=		SDL_image-1.2.12.tar.gz:main
DF_INDEX=		1
SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

BUILDRUN_DEPENDS=	tiff:primary:standard
			WebP:single:standard

USES=			cpe gmake libtool pkgconfig jpeg png fbsd10fix
SDL_COMPONENTS=		sdl1

DISTNAME=		SDL_image-1.2.12

CPE_PRODUCT=		sdl_image
CPE_VENDOR=		libsdl
FPC_EQUIVALENT=		graphics/sdl_image

MUST_CONFIGURE=		gnu
CONFIGURE_ARGS=		--enable-jpg
			--enable-png
			--enable-tif
			--enable-webp

INSTALL_TARGET=		install-strip

LDFLAGS=		-lm

post-install:
	${INSTALL_PROGRAM} ${WRKSRC}/.libs/showimage \
		${STAGEDIR}${PREFIX}/bin

[FILE:227:descriptions/desc.single]
SDL_image is an image loading library that is used with the SDL library,
and almost as portable. It allows a programmer to use multiple image
formats without having to code all the loading and conversion
algorithms themselves.


[FILE:102:distinfo]
0b90722984561004de84847744d566809dbb9daf732a9e503b91a1b5a84e5699      2231074 SDL_image-1.2.12.tar.gz


[FILE:153:manifests/plist.single]
bin/showimage
include/SDL/SDL_image.h
lib/
 libSDL_image-1.2.so.0
 libSDL_image-1.2.so.0.8.4
 libSDL_image.a
 libSDL_image.so
lib/pkgconfig/SDL_image.pc

